# =============================================================================
# k8s/02-postgres.yaml — DESHABILITADO en Fase 2
#
# Este StatefulSet fue el PostgreSQL interno de K3s (single-node).
# En Fase 2 fue reemplazado por:
#   k8s/02-postgres-external.yaml  — Service + Endpoints al clúster PG HA
#
# NO eliminar este archivo en caso de necesitar rollback.
# Para reactivar: renombrar este archivo y eliminar 02-postgres-external.yaml
# =============================================================================
#
# ARCHIVO INACTIVO — apply-manifests.sh no aplica archivos sin extensión .yaml
# Renombrado a .yaml.disabled para preservar el historial.
#
# Contenido original preservado en git (ver historial de commits).
